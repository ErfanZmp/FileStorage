# Application
PORT=3001
SESSION_SECRET=replace-with-a-long-random-string
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-admin-pass
CHUNK_SIZE=8388608

# Database connection string for Prisma / app
# Local development example (ensure MariaDB is reachable on localhost)
DATABASE_URL=mysql://filemanager:change_me@127.0.0.1:3306/filemanager

# Optional: values used by docker-compose to provision MariaDB
DB_ROOT_PASSWORD=rootpassword
DB_NAME=filemanager
DB_USER=filemanager
DB_PASSWORD=change_me
